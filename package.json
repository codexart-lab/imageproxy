{
  "name": "cf-image-proxy",
  "version": "1.0.0",
  "description": "Production-ready Cloudflare Image Proxy with TypeScript",
  "main": "src/index.ts",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "build": "wrangler deploy --dry-run",
    "tail": "wrangler tail"
  },
  "keywords": [
    "cloudflare",
    "workers",
    "image-proxy",
    "typescript",
    "image-resizing"
  ],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240620.0",
    "typescript": "^5.5.2",
    "wrangler": "^3.63.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
